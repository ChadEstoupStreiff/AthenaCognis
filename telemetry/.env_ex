# Telemetry service ports
TELEMETRY_PORT=8403
TELEMETRY_DASHBOARD_PORT=8404

# Path where the SQLite database will be stored (must exist on the host)
DATA_PATH=/your/data/path

# Generate one with `openssl rand -hex 32` and keep it secret.
TELEMETRY_SECRET=XXXXX